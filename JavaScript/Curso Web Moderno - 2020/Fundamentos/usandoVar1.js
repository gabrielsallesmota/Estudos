{ 
    { 
        { 
            { 
                var sera = 'Sera????' 
                console.log(sera)
            } 
        } 
    }
}     

console.log(sera)

function teste(){
    var local = 123
    console.log (local)
}

teste ()

//quando se define uma variavel dentro da função,ele estará diponível apenas no escopo da função;
//se deve evitar criar uma variavel no escopo global por qualquer sobscrita sobre a variável ele aceita;
// variavel existe dois tipos, ou variavel global ou então variavel no escopo de função (que fica dentro de uma função)