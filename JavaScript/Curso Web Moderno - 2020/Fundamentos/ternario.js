const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado'  //primeiro => é da função

//controlador ternário é composto por 3 partes: Expressão que retorna verdadeiro ou falso, primeira parte caso seja verdadeiro, e segunda caso seja falso

console.log (resultado(7.1))
console.log (resultado(6.7))